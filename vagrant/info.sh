#!/usr/bin/env bash
echo "========================================================================"
ifconfig | grep inet
ip addr  | grep inet
hostname -I
echo "========================================================================"
